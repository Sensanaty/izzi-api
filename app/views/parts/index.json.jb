metadata = {
  **@metadata,
  count: @pagy.count
}

parts = @parts.map do |part|
  part.attributes.merge(
    company_name: part.company.name
  )
end

{
  data: parts,
  metadata:,
  token: @new_token
}
